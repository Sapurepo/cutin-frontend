# 프론트 단독 개발: MSW 핸들러 기반 in-process 모킹 (env 변경 시 Metro 재시작 필요)
EXPO_PUBLIC_API_MOCKING=enabled

# 실백엔드 전환 시에만 설정 — 모킹 중엔 in-process 인터셉트라 실기기에서도 기본값(localhost) 그대로 동작
# EXPO_PUBLIC_API_URL=http://localhost:8080/api/v1
